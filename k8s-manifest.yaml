Manifiesto para el despliegue y servicio de la aplicación Flask
apiVersion: apps/v1 kind: Deployment metadata: name: flask-app-deployment spec: replicas: 3 selector: matchLabels: app: flask-app template: metadata: labels: app: flask-app spec: containers: - name: flask-app image: <REEMPLAZAR_IMAGEN> ports: - containerPort: 5000
Manifiesto para exponer la aplicación con un LoadBalancer
apiVersion: v1
kind: Service
metadata:
name: flask-app-service
spec:
type: LoadBalancer
ports:

port: 80
targetPort: 5000
selector:
app: flask-app